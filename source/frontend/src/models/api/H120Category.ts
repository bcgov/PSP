import { Api_ConcurrentVersion } from './ConcurrentVersion';

export interface Api_H120Category extends Api_ConcurrentVersion {
  financialActivityId: number | null;
  description: string | null;
  h120CategoryNo: number | null;
}
