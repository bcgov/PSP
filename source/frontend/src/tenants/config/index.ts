import { ITenantConfig2 } from '@/hooks/pims-api/interfaces/ITenantConfig';

import { config as MOTI } from './MOTI';

/**
 * Pre-configured tenant settings.
 */
export const config: Record<string, ITenantConfig2> = {
  MOTI,
};
